server:
  port: 7001
---
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitshell.com/madx/config.git
          username: username
          password: password
          search-paths: cherry/{application}/

